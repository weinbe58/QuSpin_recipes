{% set version = "0.3.1" %}

package:
  name: quspin
  version: {{ version }}

source:
  url: https://github.com/weinbe58/QuSpin/archive/dev_{{ version }}.zip

build:
  number: {{ PKG_BUILDNUM }}
  string: py{{ py_version | replace(".", "") }}h{{PKG_HASH}}_{{ PKG_BUILDNUM }}
  script: python setup.py install

requirements:
  build:
    - {{compiler('cxx')}}

  host:
    - python {{ py_version }}
    - numpy 1.11.*
 
  run:
    - python {{ py_version }}
    - {{ pin_compatible('numpy') }}
    - scipy >=0.19.0
    - six
    - joblib
    - dill
    - gmpy2 
    - numba
    
test:
  imports:
    - quspin 

about:
  home: https://github.com/weinbe58/QuSpin.git
  license: BSD-3
